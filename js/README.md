# js

This directory contains a minimal harness to launch Minenode from JavaScript. The example can be loaded from NodeJS or Bun (preferred) via:

```shell
node index.ts
bun index.ts
```

See `crates/minenode_napi` for the bindings.
